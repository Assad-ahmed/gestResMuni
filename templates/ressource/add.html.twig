{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.nomRessource) }}</div>
        <div class="col">{{ form_row(form.type) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block PageTitre %}
    Créér d'une ressourece propre
{% endblock %}

{% block titreitre %}
   formulaire d'une ressource propre
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}

