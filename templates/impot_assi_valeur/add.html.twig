{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.typeImpotAssi) }}</div>
        <div class="col">{{ form_row(form.MontantCumule) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.date) }}</div>
        <div class="col">{{ form_row(form.typeImpots) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}
{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}

{% block title %}
{% endblock %}
{% block PageTitre %}
{% endblock %}

