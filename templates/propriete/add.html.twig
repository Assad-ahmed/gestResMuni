{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.type) }}</div>
        <div class="col">{{ form_row(form.region) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.revenuNet) }}</div>
        <div class="col">{{ form_row(form.valeurVenale) }}</div>
        <div class="col">{{ form_row(form.valeurLocative) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block title %}

{% endblock %}

