{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.montantCumuleJournalier) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.montantCumuleMensuel) }}</div>
        <div class="col">{{ form_row(form.montantCumuleAnnuel) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block title %}

{% endblock %}