{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.montant) }}</div>
       
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.date) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block PageTitre %}
    Créér d'une excedent de clôture ordinaires
{% endblock %}

{% block titre%}
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}


