{% extends '/template.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.nomP) }}</div>
        <div class="col">{{ form_row(form.typeBatiment) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.surface) }}</div>
        <div class="col">{{ form_row(form.valeurLocative) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.adresse) }}</div>
        <div class="col">{{ form_row(form.dateImposition) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block title %}

{% endblock %}

{% block PageTitre  %}
    <h1>Create new CFPB</h1>
{% endblock %}
