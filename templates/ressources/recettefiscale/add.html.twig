{% extends 'template.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.montantCumuleJour) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.montantCumuleMoi) }}</div>
        <div class="col">{{ form_row(form.montantCumuleAnnee) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block PageTitre %}
    Créér d'une recette fiscale
{% endblock %}

{% block titreitre %}
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}

