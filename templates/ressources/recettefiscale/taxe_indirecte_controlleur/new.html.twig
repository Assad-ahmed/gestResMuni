{% extends '/template.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.typeTaxe) }}</div>
        <div class="col">{{ form_row(form.codeArticle) }}</div>
        <div class="col">{{ form_row(form.description) }}</div>
    </div>
    <div class="row">
        <div class="col">{{ form_row(form.date) }}</div>
        <div class="col">{{ form_row(form.montantJournalier) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}

{% block title %}

{% endblock %}

{% block PageTitre  %}
    <h1>Create new CFPNB</h1>
{% endblock %}
